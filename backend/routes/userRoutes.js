// import express from 'express';
// import { createUser } from '../controllers/userController.js';
// import { verifyJWT, verifyAdmin } from '../middlewares/authMiddleware.js';

// const router = express.Router();

// router.post('/new-user', createUser);
// router.get('/users', verifyJWT, verifyAdmin, userController.getUsers);
// router.get('/users/:id', verifyJWT, verifyAdmin, userController.getUserById);
// router.delete('/delete-user/:id', verifyJWT, verifyAdmin, userController.deleteUser);
// router.put('/update-user/:id', verifyJWT, verifyAdmin, userController.updateUser);

// export default router;
